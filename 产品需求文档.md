# 产品需求文档｜Markdown 驱动的智能 GTD 工作空间

| 项目 | 内容 |
| --- | --- |
| 文档版本 | V1.0，开发基线建议稿 |
| 更新日期 | 2026-09-10（补充实现状态与 D-03 修订，V1 需求基线不变） |
| 产品阶段 | 0.1.0 本地 MVP 已实现；完整 V1 尚未发布 |
| 开发投入 | 个人开发为主，AI 辅助；已根据本次沟通确定 |
| 主入口 | 按草稿以微信小程序优先；具体平台决策见第 15 节 |
| 配套文档 | [开发计划](开发计划.md)、[原始 V0.1 草稿](docs/archive/产品需求文档_V0.1.md) |

本文件说明“做什么、遵守什么规则、如何验收”。开发顺序、技术实现与工作量见开发计划。文档 V1.0 不代表产品已经发布；除用户已明确的人员投入外，新增技术选择、容量与指标均为可评审的建议基线。

当前代码、运行方式和已验证范围分别见 [README](README.md)、[MVP 验收记录](docs/MVP验收记录.md) 与 [实现决策](docs/实现决策.md)。本地 MVP 默认使用规则解析与 SQLite；微信真机、真实模型和生产发布条件尚未验收，不能据此把下述全部 V1 需求标为完成。

## 1. 产品定位与目标

产品是一个以 Markdown 保存工作内容的个人 GTD 工作空间。用户先把文字、粘贴的微信内容和后续支持的录音放进 Inbox，再由 AI 提取任务、推荐文件归属和变更内容，用户确认后写入文件。

核心闭环：收集 → 澄清 → 组织 → 执行 → 回顾。V1 优先交付前四步；周期回顾后续增强。

### 1.1 核心价值

- 收集时只要求输入内容，不强制填写项目、标签和日期。
- 保留原始 Capture、任务和文件间的来源关系。
- Folder 表达归属，Task Group 表达工作单元，Link 表达跨文件关系。
- 文件内容可阅读、导出和重建索引，避免关键业务内容只存在数据库中。
- AI 提供建议；用户能预览、修改、取消，并撤销已经执行的变更。

### 1.2 产品成功标准

V1 内测以 5—10 位目标用户、至少 7 天使用记录验证以下目标：

| 指标 | 建议目标 | 统计口径 |
| --- | --- | --- |
| 快速收集 | 输入完成后 5 秒内完成提交操作 | 不含用户打字时间；网络耗时另计 |
| 输入可靠性 | 已返回“保存成功”的 Capture 不丢失 | 用持久化、崩溃恢复和备份演练验证 |
| 核心流程完成 | 至少 80% 内测用户独立完成一次完整闭环 | 收集、整理、确认写入、完成任务、撤销 |
| 提案可用性 | 至少 70% 提案只需修改少量字段即可接受 | 按第 13 节口径记录，不能用确认率代替准确率 |
| 数据可恢复性 | 发布验收用例中的错误写入均可恢复 | 包括重试、冲突、撤销及进程中断 |

这些目标是评估标准，不是已达成的数据，也不是外部服务承诺。

## 2. 用户、场景与边界

### 2.1 用户与权限

首批用户为需要处理大量沟通、方案、客户跟进和个人事项的知识工作者。V1 每个账号创建一个私有 Workspace；服务可容纳多个账号，但不支持共享工作空间或团队角色。

任务中的“负责人”只是业务字段，可以是“我”、联系人文字或空值，不授予登录或文件访问权限。

### 2.2 主要用户故事

| 编号 | 场景 | 预期结果 | 对应需求 |
| --- | --- | --- | --- |
| US-01 | 输入“明天下午给李总发合同” | 立即保存原文，提取任务与相对日期，未提到的 Deadline 保持空值 | FR-001、FR-007、FR-008 |
| US-02 | 一段文字包含三个待办 | 提取三项任务，展示归属建议，确认后一次写入 | FR-007、FR-011、FR-012、FR-027 |
| US-03 | 为已有方案补充售后章节 | 展示候选文件与追加位置，保留已有正文 | FR-012、FR-027 |
| US-04 | AI 超时或手机断网 | 已保存原文可查看；未上传草稿可重试，不出现重复 Capture | FR-006、FR-028、FR-032、FR-037 |
| US-05 | 发现刚才写入的任务有误 | 查看差异并撤销；文件发生后续变化时提示冲突 | FR-014、FR-028 |
| US-06 | 通过录音记录会议行动项 | 音频先保存，再转写与提取；失败仍可播放原音频 | FR-002，V3 |

### 2.3 不纳入 V1 的事项

录音与转写、批量 Capture 合并分析、AI 子任务拆解、依赖图、语义检索、团队协作、提醒推送、订阅付费、外部日历双向同步、本地或 Git 双向同步均不作为 V1 发布条件。

微信内容通过用户主动粘贴进入系统；任何版本都不以读取个人微信私聊记录为前提。后续平台接入以实际开放能力和必要授权为准。

## 3. 版本划分与优先级

P0 表示该版本发布必需，P1 表示后续增强，P2 表示实验性能力。相同需求分阶段交付时，以本表和第 8 节明确的子范围为准。

| 产品版本 | 交付目标 | 范围 |
| --- | --- | --- |
| V0 | 数据与流程验证 | Schema、Markdown 格式、页面流程、样例集、人工评估 |
| V1 / P0 | 文字任务闭环 | 登录隔离、文字 Inbox、AI 提案、确认写入、任务管理、基础目录/标签/搜索、版本撤销、导出与恢复 |
| V2 / P1 | 任务组与关系 | 批量整理、子任务、依赖与反向链接、标签批量管理、Markdown 导入和完整源码编辑 |
| V3 / P1 | 录音入口 | 录音、上传、转写、原音频追溯和失败恢复 |
| V4 / P1 | 微信工作流扩展 | 验证可行的分享入口、企业微信接入、批量聊天整理 |
| V5 / P2 | 回顾与智能辅助 | 每日/每周回顾、长期未处理事项、重复任务建议、语义检索 |

V2 以后按真实使用数据重新排序，不因预留字段而提前实现整套功能。

## 4. 信息架构与页面规格

V1 底部导航沿用草稿：Inbox、今日、工作空间、搜索、我的。确认页、任务详情和版本历史为二级页面。

| 页面 | 必须呈现的内容 | 主要操作与状态 |
| --- | --- | --- |
| 登录与首次使用 | 产品用途、私有工作空间、AI 数据处理说明 | 登录、设置时区、决定是否启用外部 AI；关闭 AI 后仍可手工使用 |
| Inbox | 输入框、来源、时间、整理状态、失败原因 | 保存、立即整理、稍后处理、查看原文；分别显示“仅本机草稿”和“已保存到云端” |
| AI 确认页 | 原文、任务字段、候选文件、差异、待澄清项 | 改字段、改归属、选取动作、确认、取消；显示本次影响文件数和任务数 |
| 今日 | 今日计划、今日到期、逾期、下一步/等待/将来筛选 | 完成、延期、打开详情；空列表提供快速记录入口 |
| 工作空间 | 面包屑、文件夹、任务组列表、归档入口 | 创建、重命名、移动、归档；手机逐层浏览，不展开无限目录树 |
| Task Group / Task | 正文、任务、属性、来源、版本 | 结构化编辑、完成、延期、编辑非托管正文、只读源码预览 |
| 搜索 | 查询词、过滤条件、结果片段和对象类型 | 打开任务/文件/原文；区分无结果、索引处理中、查询失败 |
| 我的 | 时区、AI 开关与用量、导出、回收站、账户 | 导出、恢复、删除数据、退出；重要动作再次显示影响范围 |
| 版本历史 | 时间、来源、操作者、差异、恢复目标 | 撤销最近变更、恢复历史内容；冲突时先预览新的恢复方案 |

通用规则：正文支持中文与长文本；交互目标建议不小于 44×44 逻辑像素；状态不能只依赖颜色。加载、空态、错误、无权限、离线和重试均有明确文案。提交中阻止重复点击，同时依靠服务端幂等保障结果。

V1 不承诺在微信后台持续运行；回到前台时重新查询任务状态。

## 5. 领域模型与业务规则

### 5.1 对象职责

| 对象 | 作用 | 关键约束 |
| --- | --- | --- |
| Workspace | 账号的私有工作空间 | 所有资源访问必须校验所属 Workspace |
| Folder | 用户定义的组织容器 | 只有一个父目录；移动后不能形成目录循环 |
| Capture | 尚未或已经整理的原始输入 | 原文不可被 AI 改写；重新分析形成新的提案 |
| Task Group | 一个 Markdown 文件代表的工作单元 | 包含零到多个 Task；零任务时可以作为资料记录 |
| Task | 最小可执行行动 | 只属于一个主 Task Group，ID 在移动和改名后保持不变 |
| Proposal | 对一个或多个 Capture 的结构化变更建议 | 内容有修订号；确认绑定具体修订和文件版本 |
| Change Set | 一次实际执行的变更批次 | 记录前后快照、幂等标识、结果和恢复状态 |
| Relation | 文件或 Task 间的关系 | V2 引入；不能越过 Workspace 边界 |
| Job | 可重试的后台处理 | 与业务对象分离，记录尝试次数、心跳和失败原因 |

生产 ID 采用对象前缀加 UUID，例如 `task-6b74f8c1-748d-4264-b613-3e5e7335a421`。任务标题或文件名不能作为主键。

### 5.2 Task 字段

| 字段 | 类型与默认值 | 规则 |
| --- | --- | --- |
| id / group_id | ID；必需 | API 返回 group_id；文件中的 Task 从宿主文件推导 group_id |
| title | 1—200 个字符 | 行动描述；人工可修改，不强制 AI 改写原意 |
| status | 枚举；默认 next | 取值见下节 |
| owner | 字符串或 null | AI 未识别到负责人时留空；“默认我”必须作为用户设置明确展示 |
| scheduled / deadline | TimeSpec 或 null | 计划执行时间与截止时间独立，不互相推导 |
| priority | low / medium / high；默认 medium | 无依据不推断为 high |
| location / context | 字符串或 null | 地点与执行场景可选 |
| tags | 字符串数组；默认空 | 去首尾空格、去重；大小写和中文原样展示 |
| parent_task_id | ID 或 null | V2 同组父任务；禁止循环 |
| depends_on | ID 数组；默认空 | V2 可跨组、不可跨工作空间；禁止循环依赖 |
| source_refs | Capture ID 数组 | 统一草稿中的 source 表达，支持后续多来源 |
| created_at / updated_at | ISO 8601 时间戳 | 服务端生成，存储 UTC |
| completed_at | 时间戳或 null | 完成时记录，重新打开时清空；历史事件仍保留 |
| archived_from_status | 状态或 null | 归档时保存，恢复时使用 |

Task Group 至少包含 `schema_version`、`id`、`title`、`status`、`created_at`、`updated_at`、`revision` 和 `tasks`；可附带 owner、tags、group_type、project、area 与 related。文件夹属性继承暂不启用，避免移动目录隐式改变任务。

### 5.3 状态与转换

Capture 状态：

- `unprocessed → processing → needs_confirmation → processed` 为主流程。
- 分析失败进入 `failed`，原文仍可查看；重试回到 processing。
- 用户拒绝提案后回到 unprocessed；归档进入 archived，可恢复到归档前状态。
- processed 的 Capture 不再一键重复整理；“重新分析”显式创建新提案并提示已有产物。

Proposal 状态：`pending_confirmation → applying → applied`；其他结果为 rejected、stale、failed。修改提案增加 revision，旧修订立即失效。执行失败时必须先完成回滚或恢复判定，再允许重试。

Task 状态沿用草稿：

| 状态 | 含义 | 转换规则 |
| --- | --- | --- |
| inbox | 已生成但仍待澄清的 Task | 与尚未整理的 Capture 不同 |
| next / active | 可执行 / 进行中 | 用户手动切换，不因到点自动开始 |
| waiting / someday | 等待他人 / 将来考虑 | 可手动转回 next |
| scheduled | 已安排执行时间 | 使用此状态必须有 scheduled |
| blocked | 被阻塞 | V1 可手动标记；自动依赖判断在 V2 |
| done | 完成 | 写入 completed_at，并同步复选框 |
| cancelled | 取消 | 保留记录，不记入完成数 |
| archived | 归档 | 默认列表隐藏；恢复归档前状态 |

未终结状态之间可手动转换，但遵守字段校验；任何未终结状态可完成或取消。done/cancelled 重新打开默认回到 next。归档不会删除内容。

Task Group 状态为 active、done、archived。完成一个 Task 不会自动完成整个文件；标记组完成前必须不存在未完成 Task。向 done 组添加 Task 时，预览中同时提示将组恢复为 active；向 archived 组写入需先恢复。进度分母排除 cancelled、archived；零有效任务显示“无任务”，不显示 100%。

### 5.4 时间与 GTD 视图

TimeSpec 固定包含 `date`（YYYY-MM-DD）、`time`（HH:mm 或 null）、`period`（morning/afternoon/evening 或 null）和 `timezone`（IANA 时区）。time 与 period 互斥；deadline 不接受 period。

- 工作空间初始时区建议 Asia/Shanghai，首次使用可修改。审计时间存 UTC，任务的本地日期语义保留。
- “明天”以本次分析的 reference_time 和时区解析，默认取 Capture 保存时间；粘贴历史聊天时允许用户改参照时间。原始 Capture 的时间不改写，覆盖值记录在分析任务和 Proposal 中。
- “明天下午”保存日期和 afternoon，不擅自填 14:00；“尽快”“下周找时间”保留原句并待澄清。
- 仅有日期的 Deadline，在该日期的下一天 00:00 才判为逾期；有时间则按指定时间判断。
- 时区设置变化不自动重写已有任务的 TimeSpec；修改旧任务的时区需显式操作。
- 今日列表为 scheduled.date 为今天或 deadline.date 为今天的未终结任务；已逾期任务单列，同一页面去重。判断“今天”时先把当前时刻换算到该字段的 timezone；与工作空间当前时区不同的任务标示其时区。
- 不存在或有夏令时歧义的本地时刻在 V1 要求重新选择，不能静默换算到另一个时间。
- 逾期只由 Deadline 判断，不能因计划日期已过就判为逾期。下一步、等待、将来视图分别筛选 next、waiting、someday。
- 不同筛选字段取 AND，同字段多选取 OR；排序为到期时间、优先级、更新时间、ID，空到期时间排最后。

## 6. Markdown 持久化规范

### 6.1 唯一标准与编辑边界

采用标准 YAML Front Matter 加 Markdown 正文，`schema_version: 1`。Task 结构化字段集中放在 `tasks` 数组中，作为机器读取的唯一权威表示；正文内的任务复选框是同步生成的阅读视图。

V1 支持结构化任务编辑和非托管正文编辑，完整 Markdown 源码只读。V2 开放导入与完整源码编辑时，必须检测 YAML 和正文任务视图是否冲突，让用户选择修复，不能静默覆盖任一侧。

应用只重写需要变更的字段和托管任务区，保留无关正文、未知合法 Front Matter 字段和非托管区内容。V0 必须验证解析和序列化往返行为；不得用整文件字符串替换拼接任务。

### 6.2 完整文件示例

~~~markdown
---
schema_version: 1
id: group-83971166-997a-4eed-84b5-f46f75897195
title: 客户合同跟进
status: active
revision: 1
created_at: "2026-09-07T02:00:00Z"
updated_at: "2026-09-07T02:00:00Z"
tags:
  - 客户跟进
tasks:
  - id: task-6b74f8c1-748d-4264-b613-3e5e7335a421
    title: 给李总发合同
    status: next
    owner: null
    scheduled:
      date: "2026-09-08"
      time: null
      period: afternoon
      timezone: Asia/Shanghai
    deadline: null
    priority: medium
    location: null
    context: null
    tags:
      - 沟通
      - 合同
    parent_task_id: null
    depends_on: []
    source_refs:
      - capture-31fa8b71-6fbf-4ca4-96ed-e67624c2a80d
    created_at: "2026-09-07T02:00:00Z"
    updated_at: "2026-09-07T02:00:00Z"
    completed_at: null
    archived_from_status: null
---

# 客户合同跟进

## 工作目标

向客户发送合同并保留沟通记录。

<!-- gtd:tasks:start -->
## Tasks

- [ ] 给李总发合同 <!-- task:task-6b74f8c1-748d-4264-b613-3e5e7335a421 -->
<!-- gtd:tasks:end -->

## 执行记录

暂无补充记录。
~~~

### 6.3 文件、路径与关联规则

- 文件名来源于主题，服务端处理非法字符；同名文件提供序号后缀或让用户选择，不能覆盖已有文件。
- ID 与路径分离。重命名、移动不改变 Task/Group ID。
- V1 使用相对 Markdown 链接展示引用；V2 提供以 ID 为依据的关系与反向链接。已有引用随移动更新，未解析的普通文本链接显示告警。
- 为跨平台导出，禁止路径穿越、绝对路径、控制字符、Windows 保留名称及非法文件名字符；目录不能通过软链接越出 Workspace。
- 建议容量防护为目录深度 32、单段名称不超过 100 字符且不超过 240 UTF-8 字节、相对路径不超过 1,024 UTF-8 字节。它们是实现防护上限，不代表固定项目层级，见 D-13。
- YAML 只接受普通标量、数组和映射，拒绝自定义类型、循环引用和重复关键键；未知 schema_version 拒绝写入并提示版本不兼容。

## 7. 核心流程与异常处理

### 7.1 从收集到写入

~~~mermaid
flowchart TD
  A["用户输入文字或粘贴内容"] --> B["本机保存待上传草稿"]
  B --> C["服务端持久化 Capture"]
  C --> D["用户选择立即整理或稍后处理"]
  D --> E["后台提取并生成 Proposal"]
  E --> F["用户预览、编辑、选择动作"]
  F --> G{"确认修订与文件版本是否仍有效"}
  G -- 是 --> H["记录 Change Set 和快照"]
  H --> I["执行文件变更并提交索引"]
  I --> J["展示成功结果与撤销入口"]
  G -- 否 --> K["重新展示差异并再次确认"]
  E -- 失败 --> L["保留原文，允许重试或手工整理"]
~~~

保存 Capture 是用户已经发起的操作，立即执行；它不需要额外等待 AI 确认。AI 对 Task Group 的变更才走提案确认流程。用户手动编辑任务的“保存”视为对该次明确变更的确认。

创建 Task 时，必须将它放入 Workspace 根目录或文件夹中的 Task Group；Task 是文件内的内容，不得直接将任务标题用作新文件/任务组名称。优先复用用户选中或完整名称唯一匹配的可写任务组；没有合适的任务组则提议新建，以主题命名。本地规则无法判断主题时，使用或新建根目录的「日常任务」。用户可修改归属和名称；同一位置同名的可写任务组复用，不因重复收集而不断生成同名文件。多个匹配先澄清；归档、回收站或其他 Workspace 的对象不能自动成为目标。

### 7.2 关键场景规则

| 场景 | 产品行为 |
| --- | --- |
| 断网、提交超时 | 草稿保持待上传；使用相同 client_capture_id 重试并查询原提交结果 |
| AI 失败 | Capture 进入 failed，显示可理解的原因，不删除原文；可重试或手工建任务 |
| AI 无法确定日期、负责人或归属 | 返回空值或候选项；必需项未澄清不能确认，非必需项可留空 |
| 找到多个相似文件 | 展示最多 5 个候选及推荐依据，用户选择；不自动追加 |
| 多动作提案 | 用户可取消部分动作；确认针对最终选中集合，未选项明确标为忽略并保留记录 |
| 只确认部分内容 | 至少一个动作成功后 Capture 标为 processed；原文和被忽略建议仍可追溯，后续通过“重新分析”继续整理 |
| 确认期间文件已修改、移动或归档 | 返回冲突，提案标为 stale，展示最新目标和新差异，不自动覆盖 |
| 重复确认或客户端重试 | 返回原 Change Set 结果，不再生成新的文件、任务或版本 |
| 多文件写入中断 | 对用户呈现一个整体结果；恢复完成前禁止读取受影响内容或继续写入 |
| 撤销时存在后续编辑 | 不直接覆盖；展示恢复后的差异，形成新的、待用户确认的恢复变更 |
| 输入只是资料 | 建议保存为零任务的 Task Group，或由用户直接归档 Capture |
| 原始 Capture 被用户删除 | 关联 Task 不连带删除；来源显示“已删除”，保留不含原文的引用信息 |

用户拒绝整个提案不会改变已有业务文件。不能因为高置信度、超时或多次失败而自动跳过确认。

### 7.3 输入与批次限制

建议 V1 单条文字不超过 10,000 个 Unicode 字符且不超过 64 KiB；一个 AI 提案最多 20 个动作、影响最多 5 个文件。超限应提示分段或拆批，不能悄悄截断原文。限制由服务端统一校验，并在客户端提前提示。

V1 一次只分析一个 Capture；V2 批量分析初始上限建议为 10 条，仍受总体输入与动作上限约束。文件维护、导出等后台批量任务另有执行策略，不能误用 AI 提案限制。

## 8. 功能需求与验收标准

FR-001—FR-023 延续草稿编号；新增能力从 FR-024 开始。表中的版本表示首次交付范围，增强能力会显式标注。

### 8.1 收集与 AI 整理

| 编号 | 需求与版本 | 验收标准 |
| --- | --- | --- |
| FR-001 | 文字输入；V1/P0 | 多行文字可保存；重启后原文一致；同一 client_capture_id 重试仅产生一个 Capture |
| FR-002 | 录音输入；V3/P1 | 录音先保存后转写；中断上传可恢复；拒绝麦克风权限有文字输入入口；转写失败仍可播放原音频 |
| FR-003 | 微信文字输入；V1/P0 | 用户主动粘贴；保留原文与可识别的发送人、时间信息；不会请求读取私人聊天记录 |
| FR-004 | Inbox 列表；V1/P0 | 显示摘要、来源、时间和状态；默认不显示已归档项；分页、筛选和处理结果一致 |
| FR-005 | 批量处理；V2/P1 | 选择多条原文后统一预览提案；每个产物可追溯到具体来源；重复提交不重复写入 |
| FR-006 | 原始内容查看；V1/P0 | 从 Task 或提案可回到原文；AI 重试不修改原文；V3 增加原音频与转写版本 |
| FR-007 | Task 提取；V1/P0 | 能区分单任务、多任务、资料和待澄清；无任务时不强造待办；父子任务增强在 V2 |
| FR-008 | 属性提取；V1/P0 | scheduled 与 deadline 分开；字段附原文依据；没有依据的负责人、日期保持空值 |
| FR-009 | 置信度与澄清；V1/P0 | 每个关键字段可展示 high/medium/low；低置信项有明显提示；任何等级都需要用户确认 |
| FR-010 | AI 拆解；V2/P1 | 默认建议 3—7 个子任务，可编辑与取消；保留原 Task；确认后才保存父子关系 |
| FR-027 | 提案预览与确认；V1/P0 | 展示字段、目标和差异；编辑增加修订号；过期修订不能应用；取消不修改业务文件 |
| FR-031 | AI 授权和用量；V1/P0 | 可关闭外部 AI；发送前说明范围；显示当日用量和预算；额度不足时仍能保存原文和手工编辑 |
| FR-032 | 后台任务与重试；V1/P0 | 显示排队、运行、成功、失败；进程重启后可恢复；超过重试次数停止自动重试并提供人工入口 |

### 8.2 文件、任务与组织

| 编号 | 需求与版本 | 验收标准 |
| --- | --- | --- |
| FR-011 | 新建 Markdown；V1/P0 | 任务必须属于 Workspace 或文件夹下独立命名的任务组；缺少合适任务组时经确认创建，不照抄任务标题；包含合法 Front Matter，ID 唯一，同名不覆盖 |
| FR-012 | 追加文件；V1/P0 | 预览新增位置；保留原有任务和非托管正文；同一变更重试不重复追加 |
| FR-013 | 更新文件；V1/P0 | 结构化属性与非托管正文可编辑；只改目标内容；完整源码编辑在 V2 |
| FR-014 | 版本与撤销；V1/P0 | 每次成功变更可查看前后差异；最近变更可撤销；旧版本恢复创建新版本，保留历史 |
| FR-015 | Task 编辑；V1/P0 | 标题、状态、计划、截止时间、负责人、标签等保存后保持一致；依赖编辑在 V2 |
| FR-016 | Task 完成；V1/P0 | 完成后状态、正文复选框、完成时间和索引同步；重新打开不会丢失历史完成记录 |
| FR-017 | Task 延期；V1/P0 | 分别修改计划或 Deadline；可记录原因；历史版本能查出原值 |
| FR-018 | Task 依赖；V2/P1 | 支持同工作空间跨组依赖及同组父子关系；拒绝循环；blocked 可解释；解除依赖后不擅自完成任务 |
| FR-019 | 文件夹操作；V1/P0 | 创建、重命名、移动和归档可用；非空目录删除显示数量；冲突命名与非法移动被拒绝 |
| FR-020 | 自定义层级；V1/P0 | 不强制项目类型；逐层浏览和面包屑可用；超出实现防护上限时明确提示 |
| FR-025 | GTD 视图；V1/P0 | 今日、逾期、下一步、等待和将来按第 5.4 节计算；午夜和时区边界测试通过 |
| FR-026 | 标签；V1/P0 | 可为单个 Task/Group 添加、移除标签并筛选；全局改名、删除和合并在 V2 |
| FR-035 | 关系与反向链接；V2/P1 | 关系依据稳定 ID；移动后仍有效；反向关系自动派生；无效引用有检查报告 |

### 8.3 检索、数据与账户

| 编号 | 需求与版本 | 验收标准 |
| --- | --- | --- |
| FR-021 | 结构化筛选；V1/P0 | 状态、计划/截止日期、负责人、目录、任务组、标签、来源可组合；分页无重复、无漏项 |
| FR-022 | 全文检索；V1/P0 | 可检索中文标题、任务、正文、Capture；显示匹配片段；不能返回其他工作空间数据 |
| FR-023 | 语义检索；V5/P2 | 返回原文出处及可解释结果；明确与普通筛选的差异；检索仍遵守工作空间隔离 |
| FR-024 | 登录与工作空间隔离；V1/P0 | 首次登录创建私有空间；伪造其他 Workspace/资源 ID 返回拒绝；退出后本机不展示前一账户数据 |
| FR-028 | 幂等、冲突与恢复；V1/P0 | 相同幂等键只执行一次；旧 revision/hash 写入返回冲突；故障恢复后不存在对用户可见的半批次结果 |
| FR-029 | 数据导出；V1/P0 | 异步导出 Markdown、Capture、可用附件和清单；重新下载校验一致；下载链接有时效和鉴权 |
| FR-030 | 归档、回收站与删除；V1/P0 | 归档可恢复；删除先入回收站；永久删除显示范围并再次确认；工作空间删除遵守第 12 节时限 |
| FR-033 | 索引检查与重建；V1/P0 | 丢弃派生索引后可从内容源重建；损坏文件单独报告；重建不覆盖原文件 |
| FR-034 | Markdown 导入；V2/P1 | 先试运行再确认；报告格式、重复 ID、路径和任务视图冲突；失败不覆盖现有文件；双向同步另立项目 |
| FR-036 | 操作追溯；V1/P0 | 通过 request_id / proposal_id / change_set_id 查到来源、确认修订、操作者、时间和结果 |
| FR-037 | 首次使用与离线恢复；V1/P0 | 首次可用示例引导；未上传草稿可恢复；切换账户不会混用草稿；离线不显示云端成功 |
| FR-038 | 容量与服务状态；V1/P0 | 超限、磁盘不足、AI 不可用和索引恢复中均有明确反馈；失败不静默丢数据 |
| FR-039 | 回顾与执行辅助；V5/P2 | 用户主动开启；建议包含来源与筛选范围；改日期、合并任务仍需确认 |
| FR-040 | 微信扩展接入；V4/P1 | 先完成目标接口能力验证；用户可解绑；去重与权限校验通过；不可用的平台能力不作为已支持功能宣传 |

## 9. AI 提案协议与行为约束

### 9.1 输入与处理范围

输入由当前 Capture、用户选择的参照时间、必要的 Task/Group 元数据和检索到的少量片段组成。默认不上传整个工作空间。检索顺序为用户明确指定 → 标题/路径/标签 → 中文关键词匹配；向量召回为后续增强。

原文、文件正文和检索结果均是不可信业务数据，不能提升为系统指令。原文中的“忽略规则、删除文件、调用接口”等内容不能触发执行权限。

### 9.2 服务端提案示例

以下是接口返回的独立追加场景示例。ID、目标版本、内容哈希由服务端补充和校验，不接受模型自行决定任意文件路径。

~~~json
{
  "id": "proposal-f2d59b7b-bc0b-45bc-8459-1c2d285bdb5d",
  "revision": 1,
  "status": "pending_confirmation",
  "capture_ids": [
    "capture-31fa8b71-6fbf-4ca4-96ed-e67624c2a80d"
  ],
  "reference_time": "2026-09-07T10:00:00+08:00",
  "classification": "single_task",
  "actions": [
    {
      "action_id": "action-565f92ee-5a0d-4f2a-8e6b-a1d257e89276",
      "type": "append",
      "target_group_id": "group-83971166-997a-4eed-84b5-f46f75897195",
      "base_revision": 3,
      "base_hash": "6f9e743ef514db676cc06fca00c7d788061fc1f56390b77a3767d081c8b57b86c",
      "selected": true,
      "task": {
        "id": "task-c0b1e1de-9a44-4630-bd9b-035c8e8a2c13",
        "title": "给李总发合同",
        "status": "next",
        "owner": null,
        "scheduled": {
          "date": "2026-09-08",
          "time": null,
          "period": "afternoon",
          "timezone": "Asia/Shanghai"
        },
        "deadline": null,
        "priority": "medium",
        "tags": ["沟通", "合同"]
      },
      "field_confidence": {
        "title": "high",
        "scheduled": "high",
        "owner": "low"
      },
      "evidence": {
        "title": "明天下午给李总发合同",
        "scheduled": "明天下午"
      }
    }
  ],
  "questions": []
}
~~~

模型先返回受 JSON Schema 约束的候选内容。服务端执行字段白名单校验、来源校验和资源解析后才生成上述可确认提案；缺省可选字段按第 5.2 节补齐，时间戳在实际执行时生成。

### 9.3 动作约定

| 动作 | 必需内容 | 校验 |
| --- | --- | --- |
| create | folder_id、文件名建议、group 属性、tasks | 目录归属、路径安全、重名、Schema 和容量 |
| append | target_group_id、base_revision、base_hash、新 Task 或正文片段 | 文件版本、新 ID、重复内容提示、原文保留 |
| update | 目标 Group/Task ID、base_revision、base_hash、允许修改的字段 | 当前值、字段权限、状态合法性、预览差异 |
| link / move / split / merge | V2 后的扩展协议 | V1 的 AI 提案接口明确拒绝这些类型 |

手动目录移动等操作可以在 V1 通过明确的用户操作执行，不等同于开放 AI 动作权限。

classification 枚举为 single_task、multiple_tasks、task_group、append_content、update_content、reference、needs_clarification。questions 的每项包括 field_path、message、required；仍有 required=true 的未回答项时禁止确认。

确认请求必须指定 proposal_revision、选中 action_id 集合和 Idempotency-Key。服务端从该修订读取动作，不接受确认接口夹带新任务内容。用户修改内容先更新提案、重新预览，再确认。

保存提案时，服务端还保存目标规范化路径、目标目录版本和可写状态的快照。确认时同时校验这些快照；即使文件内容哈希未变，目录移动、重命名或归档也会使旧提案失效。该快照由服务端保管，不能由确认请求覆盖。

### 9.4 失败与成本控制

- 不合法输出最多自动修复一次；仍不合法则明确失败，不能把自然语言当成可执行指令。
- 网络、限流等暂时性故障最多尝试 3 次（含首次），退避重试；鉴权错误、权限错误和预算不足直接停止。
- 每次模型调用建议 60 秒超时；前端展示排队与处理状态，不承诺固定完成时刻。
- 记录模型标识、提示词版本、Schema 版本、耗时和实际用量；常规日志不保存原始内容。
- 相同输入的缓存必须包含 Workspace、上下文内容哈希、模型和提示词版本，避免跨用户混用或基于旧文件生成提案。
- 置信度仅表示建议可靠程度，需通过样例校准，不能把模型自报的 0.9 当作 90% 正确率。

## 10. 数据来源与一致性约束

### 10.1 数据权威边界

| 数据 | 权威保存位置 | 是否可仅从 Markdown 重建 |
| --- | --- | --- |
| Task / Task Group / 正文 | Markdown 文件及版本快照 | 是 |
| Capture 原文、来源和参照时间 | 不可变 JSON 内容文件 | 否，必须随业务内容备份 |
| 音频与附件 | V3 对象文件与元数据 | 否 |
| 用户、登录、Workspace 配置、目录 ID 注册 | 持久化业务数据库 | 否 |
| 提案、确认记录、Job、Change Set、幂等记录 | 持久化业务数据库与恢复日志 | 否 |
| 任务查询、标签统计、全文检索 | 派生数据库索引 | 是，可重建 |

“Markdown 是事实来源”特指任务业务内容，不表示用户身份、处理记录和原始输入都可以丢弃。备份必须同时覆盖内容文件和不可重建的业务数据库。

### 10.2 写入保证

1. 先校验身份、输入、当前文件 revision/hash 和幂等键。
2. 生成前后内容、差异与版本快照，记录变更批次。
3. 同工作空间写操作串行；文件通过临时文件和原子替换更新。
4. 多文件操作通过恢复日志实现对用户可见的整体成功或整体恢复，不能宣称文件系统自带多文件事务。
5. 所有文件就绪后提交派生索引、处理状态与 Change Set 结果；成功响应发出后立即读取应看见新状态。
6. 进程中断时先恢复未完成批次，再开放受影响资源。索引失败不能报告全部成功。

Change Set 覆盖文件创建、编辑、移动、归档、回收站和恢复等用户可见操作；物理永久删除单独执行并明确不可撤销。

Idempotency-Key 绑定用户、接口与请求内容。同键同内容返回原结果；同键不同内容返回冲突。Capture 的 client_capture_id 和已应用的 proposal_id + revision 在对应业务记录生命周期内保持唯一，不能仅依赖短时缓存防重。

### 10.3 撤销与恢复

撤销以整个 Change Set 为单位，生成一个新的反向变更；原历史不被删除。只有当前文件与该批次 after_hash 一致时才直接撤销，否则创建需要重新预览的恢复方案。撤销新建文件先送回收站，不立即物理删除。

可恢复窗口遵循第 12 节的留存策略。超期快照已经清理或用户明确永久删除后，应说明无法恢复，不能承诺无限期撤销。

后台发现未知外部文件修改时，暂停该文件写入并报告冲突；V1 不进行静默双向同步。

## 11. 非功能需求与量化目标

以下是发布前需要验证的建议指标。基准环境为 4 vCPU、8 GiB 内存、SSD；单 Workspace 包含 5,000 个 Markdown 文件、20,000 个 Task 和 10,000 条 Capture，10 个活跃并发用户。用例数据与机器配置须写入测试报告，不能把小数据结果外推到任意规模。

| 编号 | 类别 | V1 目标与验收方法 |
| --- | --- | --- |
| NFR-01 | Capture 保存 | 1,000 字中文输入，API P95 ≤ 1 秒，不含 AI；成功响应前完成持久化 |
| NFR-02 | 列表 | API P95 ≤ 500 毫秒；约定测试网络与中档真机上首次内容展示 ≤ 2 秒 |
| NFR-03 | 搜索 | 热索引查询 P95 ≤ 1 秒；测试中文、无结果、短词和多条件查询；冷启动另报 |
| NFR-04 | AI 处理 | 1,000 字样例的单次处理 P95 目标 ≤ 30 秒；受供应商影响，超时必须可恢复，不能阻止保存 |
| NFR-05 | 正确性 | 重复提交、并发修改、进程中断、磁盘不足、索引失败测试中无确认后丢失或半批次可见状态 |
| NFR-06 | 可恢复性 | 建议 RPO ≤ 1 小时、RTO ≤ 4 小时；以一致性备份和实际恢复演练验证 |
| NFR-07 | 隔离 | 对所有资源接口执行跨 Workspace 负向测试；内容与导出下载均不可越权 |
| NFR-08 | 迁移性 | 导出的 Markdown 可被普通编辑器阅读；清单校验通过；任务信息不依赖专有二进制格式 |
| NFR-09 | 可观测性 | 记录请求、任务、变更批次的关联 ID；错误率、队列积压、磁盘和备份失败可告警 |
| NFR-10 | 可用性 | 稳定运行后按月观察 99.5% 服务可用性目标；首次发布不声称已达成月度 SLA |
| NFR-11 | 兼容性 | 在发布时仍受支持的 iOS、Android 各至少一台真机验证；微信最低基础库版本由 M0 实测冻结 |

容量建议：单 Task Group 不超过 200 个 Task，单 Markdown 不超过 1 MiB。工作空间总容量、每日 AI 额度和上传限额均可配置。接近容量上限时展示用量；达到上限时阻止新增占用并保留查看、导出、删除入口，不能自动删原文释放空间。

## 12. 安全、隐私与数据生命周期

### 12.1 安全要求

- 登录使用微信提供的一次性登录凭据换取本系统会话；服务端密钥不得进入小程序包。
- 服务端从会话确定 user_id 与 Workspace 权限，不能信任客户端提交的资源归属。
- API 使用 HTTPS；会话、AI 密钥和对象下载凭据不得写入业务正文或普通日志。
- Markdown 渲染禁用脚本、危险 HTML 和 javascript 链接；远程图片默认不自动加载，防止泄露访问来源。
- 外部 AI 首次启用前展示供应商、发送字段、数据处理范围和已知留存策略；用户可关闭。更换供应商或扩大范围需重新说明。
- 高风险操作提供明确影响范围：永久删除、账户删除、跨多个文件的恢复和批量变更。
- 日志、测试数据、演示截图使用脱敏内容；测试不得调用真实用户工作空间。
- 导出文件采用私有存储、短时授权下载，不通过公开 URL 暴露内容。

### 12.2 留存与删除建议

| 数据 | 默认策略 |
| --- | --- |
| 当前 Markdown 与 Capture 原文 | 保留到用户主动删除；AI 成功、失败或重试都不删除 |
| 版本快照 | 至少保留最近 30 天全部版本；当前版本及仍可恢复对象的必需快照不清理 |
| 回收站 | 普通删除保留 30 天，可恢复；永久删除或到期后 7 天内完成主存储清理 |
| Proposal / Change Set / 审计 | 可追溯记录默认 90 天；长期保留的幂等标识不保存额外原文 |
| 导出包 | 生成后 24 小时失效并清理，用户可重新申请 |
| 系统备份 | 加密保存最多 30 天；备份不是面向用户的历史浏览入口 |
| 账户/Workspace 删除 | 二次确认后立即停止访问、取消待处理任务；7 天内清理主存储，申请后最多 30 天内让含该数据的备份过期 |

清理任务必须覆盖索引、缓存、附件、导出包和待执行 Job；后台执行前再次检查账号状态。整库恢复后先重放删除清单，再对外开放，避免已删除数据重新出现。

用户删除某条 Capture 时，界面说明其已生成 Task 不会连带删除。删除整个工作空间才覆盖该空间全部业务内容。第三方 AI 服务的留存条款须单独核实，不能以本系统删除时限替代供应商承诺。

### 12.3 导出范围

默认导出当前 Markdown、Capture JSON、可用附件及 manifest.json，清单含路径、ID、Schema 版本和内容校验和。用户可选择附带版本和操作记录。用户会话、密码材料和服务端密钥不进入导出包。

V1 提供可迁移内容导出及由维护者操作的系统备份恢复；用户自行导入属于 V2，不混同于 V1 的备份恢复能力。

## 13. 指标、样例与 AI 评估

### 13.1 最小观测事件

记录 capture_saved、analysis_finished、proposal_edited、proposal_confirmed、change_applied、change_undone、task_completed 和 export_finished。事件包含脱敏用户标识、对象 ID、耗时、状态和版本，不记录原始聊天或正文。

提案接受率按“确认并成功应用的提案 / 已展示给用户的有效提案”计算；取消、失败和放弃分别统计。修改幅度按字段修改、目标文件修改和动作删除分别记录，不能仅以确认按钮点击判断效果。

### 13.2 评估集

V0 准备 30 条脱敏样例；M3 完成前扩充为 80 条，其中 50 条用于开发、30 条冻结为留出评估集。每条标注参照时间、已存在文件上下文、期望分类、任务和允许为空的字段。

主场景分配建议：单任务 20、多任务 15、资料 10、追加 10、更新 5、相对时间 10、指令注入 5、重复输入 5，共 80 条；各场景均覆盖合理的异常和缺失信息。

### 13.3 发布前质量门槛

| 评估项 | 建议门槛 |
| --- | --- |
| 输出协议合法率 | 服务端接受并展示的提案 100% 通过 Schema 校验；无效输出进入失败流程 |
| 内容分类 | 留出集 Macro-F1 ≥ 0.85 |
| 任务提取 | 留出集任务级 F1 ≥ 0.85 |
| 明确日期与负责人 | 在有原文依据的字段上准确率 ≥ 95%，分别报告样本量 |
| 缺失信息 | 固定负向用例中不得编造 Deadline 或负责人；保留 null 视为正确 |
| 文件推荐 | 存在合适文件的用例中 Top-3 命中率 ≥ 80%；不存在目标时应建议新建或澄清 |
| 越权与指令注入 | 固定攻击用例中无越权读取、无未确认写入、无任意路径操作 |

指标达不到时优先缩小 AI 自动填写范围、增加澄清或更换策略；不能删除失败样例来提高分数。模型、提示词、Schema 和检索策略变更后重新运行冻结集，并保留结果对照。

## 14. 版本验收与发布准入

### 14.1 V1 必过的端到端用例

| 编号 | 场景 | 通过条件 |
| --- | --- | --- |
| AT-01 | 首次登录并保存文字 | 创建私有空间；刷新和重新登录仍能查看同一原文 |
| AT-02 | 多任务提案写入 | 预览正确；确认一次创建期望任务；再次确认返回原结果 |
| AT-03 | AI 失败和重试 | 原文不变；状态可见；恢复后不产生重复产物 |
| AT-04 | 追加到已有文件 | 原有任务、未知合法字段和非托管正文保留 |
| AT-05 | 预览后文件被修改 | 原提案被拒绝；重新预览后才可应用 |
| AT-06 | 完成、延期与今日视图 | Markdown、索引和 UI 一致；日期、午夜及时区边界正确 |
| AT-07 | 写入任一步骤崩溃 | 重启恢复后呈现全部成功或恢复到原状态，无半批次结果 |
| AT-08 | 撤销及冲突恢复 | 无后续编辑可直接撤销；有后续编辑必须重新预览 |
| AT-09 | 越权访问和危险路径 | Capture、文件、任务、Job、导出与恢复接口全部拒绝越权 |
| AT-10 | 导出与备份恢复 | 导出清单校验一致；备份可在空环境恢复，达到 RPO/RTO |
| AT-11 | 删除与账户切换 | 回收站可恢复；永久删除任务覆盖缓存等副本；切换账号不混用本机草稿 |
| AT-12 | 服务降级 | AI 不可用仍可手工操作；磁盘不足、索引恢复中都有明确反馈 |

### 14.2 发布门槛

- 所有 V1/P0 功能验收通过；协议、权限、幂等和恢复用例必须全部通过。
- 无未关闭的阻断级和高严重度缺陷；低严重度问题有范围、绕行办法和处理计划。
- AI 冻结集、性能报告、两种系统真机记录和备份恢复报告齐全。
- 开发、测试、生产配置分离；回滚流程在测试环境演练。
- 5—10 人内测至少 7 天，已确认写入丢失、越权和无法恢复的损坏事件为零。
- 小程序主体、域名、隐私声明及平台审核满足实际发布要求；平台审核时长不计作已完成的工程工作。

## 15. 决策记录与待确认事项

为使计划可执行，下表给出建议基线。除 D-16 的人员投入外，均不冒充已经获得用户确认的事实。进入相关阶段时由产品负责人记录最终选择、日期和变更原因；本次文档工作可完整交付，不必先逐项决定供应商。2026-09-10 曾按实际实现修订 D-03，修订理由记于本节末。

| 编号 | 决策 | 建议基线 | 冻结时点与影响 |
| --- | --- | --- | --- |
| D-01 | 主要客户端 | 微信小程序优先，V1 不同时建设完整 Web 端 | M0；影响前端工作量与平台手续 |
| D-02 | 业务内容事实源 | Task/Group 用 Markdown，原始 Capture 用开放 JSON 文件 | M0；身份与操作记录仍需数据库备份 |
| D-03 | Task 格式 | **已按实现修订（2026-09-10）**：正文 `gtd:task:start/end` 任务块为权威，Front Matter 只保存组属性（`schema_version: 2`）；旧格式 1 的 Front Matter `tasks` 仅作兼容读取 | M0；先做往返验证再扩展功能 |
| D-04 | Task 多重归属 | 一个主组，其他关系用链接表达 | M0；避免重复任务和完成状态分歧 |
| D-05 | 文件夹属性继承 | V1 不启用隐式继承 | V2 前复评 |
| D-06 | 高置信度批量写入 | 可一次确认多个动作，但任何置信度都不跳过确认 | M0；属于产品数据操作规则 |
| D-07 | 团队协作 | V1 私有工作空间，多人共享后续单独设计 | 共享功能立项前 |
| D-08 | 负责人 | “我”或联系人文字，无权限语义 | M0；不建设联系人邀请系统 |
| D-09 | 日历与提醒 | V1 不做推送、重复任务或日历双向同步 | 有使用数据后复评 |
| D-10 | 本地与 Git 同步 | V1 导出；V2 导入；双向同步另立方案 | 同步立项前解决冲突与权限模型 |
| D-11 | 语音供应商 | V3 用适配层，先验证真机、语言、延迟与费用 | V3 开始前 |
| D-12 | 外部 AI | 可配置供应商、明确授权、可关闭；开发默认模拟服务 | M0 确定选择标准，真实调用前确定处理范围 |
| D-13 | 目录与容量防护 | 采用第 6、7、11 节建议上限，压测后调整 | M0/发布前；替代草稿“无限层级”的实现承诺 |
| D-14 | 其他输入 | 图片、PDF、附件理解在 V3 后另行评估 | 不影响文字 MVP |
| D-15 | 技术栈 | 原生小程序 TypeScript + Python/FastAPI + PostgreSQL | M0；详见开发计划，可在编码前调整 |
| D-16 | 人员与排期口径 | 已确定个人开发、AI 辅助；投入小时数尚未指定 | 计划按每周 4 个有效人日估算 |
| D-17 | 费用与商业模式 | 内测先设置资源和 AI 预算，不建设支付 | 真实服务采购前明确月预算 |
| D-18 | 发布基础设施 | 准备小程序主体/AppID、域名、HTTPS、部署区域与备份位置 | M0 开始核查，M5 前落实；外部手续不承诺时长 |

V0.1 到本基线的主要调整：固定 Task 持久化格式；补充 Proposal/Change Set/Job；保留原 FR 编号并明确延期范围；把账户隔离、幂等、备份、删除、导出和验收补入 V1；将业务开放性与不可重建的运维数据分开描述。

**2026-09-10 修订记录**：D-03 由“Front Matter `tasks` 为权威”修订为“正文任务块为权威（schema_version 2）”，以对齐实际存储实现；详见 [实现决策](docs/实现决策.md) 第 2 节与 [MVP 验收记录](docs/MVP验收记录.md)。本节其余决策仍为建议基线，未逐项获得产品负责人确认。

## 16. 范围变更与维护规则

新增需求应写明用户场景、所属版本、验收标准、工作量和依赖，并分配新 FR 编号，不复用已存在编号。调整既有需求时同时更新开发计划、测试映射与决策记录。

新增一个 V1/P0 能力，需要明确替换、延期或增加的工作量；不允许以“顺手加上”消耗发布缓冲。技术重构只有在解决已记录风险或支撑当前需求时进入主计划。

需求冻结后，每个里程碑结束进行一次短评审：已交付内容、验收证据、未解决问题、剩余工作量。AI 辅助生成的代码、测试和文档均由开发者审查后纳入成果。

## 17. 术语

| 术语 | 含义 |
| --- | --- |
| GTD | 收集、澄清、组织、回顾和执行的任务管理方法 |
| Capture | 用户提交的原始信息，不等于已经确认的 Task |
| Task Group | 一个 Markdown 文件承载的工作单元 |
| Proposal | 待用户确认的结构化建议，不表示已经写入 |
| Change Set | 已开始执行或已完成的一次业务变更批次 |
| 幂等 | 同一请求重试不会重复产生业务结果 |
| 派生索引 | 可从权威内容重新生成的查询数据 |
| RPO / RTO | 可接受的数据恢复点间隔 / 恢复服务所需时间 |
