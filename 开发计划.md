# 开发计划｜Markdown 驱动的智能 GTD 工作空间

| 项目 | 内容 |
| --- | --- |
| 版本与日期 | V1.0，2026-09-10 更新 MVP 实现状态与验证数字 |
| 配套需求 | [产品需求文档 V1.0](产品需求文档.md) |
| 原始依据 | [V0.1 草稿](docs/archive/产品需求文档_V0.1.md) |
| 执行模式 | 个人开发为主，AI 辅助 |
| 当前状态 | 0.1.0 本地 MVP 已实现；类型、单测、构建与后端回归通过，端到端 34/35（导出下载缺陷已定位待修）；完整 V1 尚未验收 |
| 排期性质 | 估算基线，按实际投入和里程碑结果滚动修订 |

本计划保留完整 V1 的方案、估算和发布门槛；第 1 节起的建议目录、接口及选型不自动等同于当前实现。实际目录与命令见 [README](README.md)，已验证范围见 [MVP 验收记录](docs/MVP验收记录.md)，取舍见 [实现决策](docs/实现决策.md)。

## 0. 当前交付与下一步（2026-09-10 更新）

已完成最小文字闭环：开发账号与隔离 → 保存 Capture → 本地规则/外部适配提案 → 编辑和确认 → Markdown 写入 → 任务完成、搜索、撤销、版本历史及导出。提供原生微信小程序和附加 Web 联调工作台，使用 SQLite、本地文件和进程内持久队列；停服备份可恢复到新目录。

2026-09-09 补充创建规则：任务不直接成为文件标题，优先归入已有可写任务组，没有合适容器时在 Workspace 根目录或选定文件夹下新建主题任务组；本地无法确定主题时使用「日常任务」。回归须覆盖容器复用、文件夹归属、同名澄清、权限/归档隔离、确认前零业务写入及并发提案重新预览。完整项目层级和混合 Markdown 块编辑界面仍单独跟踪，不因这项创建规则调整视为全部完成。

`npm.cmd run verify` 于 2026-09-10 完整执行：后端 pytest 123 项通过、共享客户端 Vitest 38 项通过、两端类型检查与构建通过；端到端 35 项中 **34 项通过、1 项失败**——失败仍是浏览器导出下载得到 0 字节。已定位根因：Chromium 会把带 `Content-Disposition: attachment` 的 `fetch()` 响应转为下载事件，并向页面返回合成的 204 空响应（与响应类型及 `acceptDownloads` 无关，`page.route` 介入时消失）。该问题使 `GET /api/exports/{id}/download` 经由 `fetch` 在浏览器中不可用，不只是测试缺陷；修复方案见 [MVP 验收记录](docs/MVP验收记录.md)。未放宽任何 ZIP/SHA-256 断言。

小程序端到端是页面控制器调用真实 API，不能替代官方工具和真机。以上结果不构成 M0—M5 的关闭依据，也不证明覆盖率、性能或安全指标。

后续按以下顺序推进，仍由个人开发、AI 辅助；外部条件未具备前不承诺发布日期。次序 1、2 所需 AppID 与外部 AI 凭据仍缺失，本阶段实际可推进的是次序 3—5 中不依赖外部资源的部分：

| 次序 | 工作包 | 完成条件 |
| --- | --- | --- |
| 1 | 小程序平台联调 | 提供 AppID/主体及 HTTPS 域名；微信登录、网络、iOS/Android 核心流程通过 |
| 2 | 真实模型验收 | 确认供应商、隐私与预算；官方协议和真实调用通过，冻结样例集出具质量报告 |
| 3 | 生产工程补齐 | 迁移、依赖锁、CI、部署和数据库方案验证；版本升级有可执行回退流程 |
| 4 | 数据生命周期与运行保障 | 永久删除/注销、保留清理、加密备份、监控和恢复时效演练完成 |
| 5 | 发布候选验收 | 压测、覆盖率、安全检查与至少 7 天真实内测后，逐项核对 PRD 第 14 节 |

次序 1、2 在 AppID、外部 AI 凭据和平台资源到位前无法启动，其等待时间不计入人日。后续完整 V1 工作量需以本 MVP 的实际取舍重新估算，不能简单扣减原始人日表。

## 1. 交付策略与估算口径

### 1.1 先完成可验证的闭环

实施顺序为：格式与契约 → 保存原文 → 手动编辑与可靠写入 → AI 提案与确认 → 任务视图和搜索 → 完整内测。

先证明数据可以正确保存、重试和恢复，再让 AI 生成更多写入动作。AI 不可用时，文字收集、手工编辑、搜索和导出仍然可用。

建议交付节点：

- 第 2 周内：可评审的页面流程、Schema、Markdown 样例和模拟交互。
- 第 7 周左右：收集、手工建任务、保存、完成和撤销的可运行闭环。
- 第 10 周左右：真实或模拟 AI 完成“提案—确认—写入”闭环。
- 第 13 周左右：具备完整 V1 功能的内测候选版本。
- 第 15 周左右：主要工程验收完成；第 16—18 周用于修复、内测观察和发布缓冲。

以上为相对周数，W1 从正式开始实施计算，不假设今天已经开工。

### 1.2 人力与工期

一个有效人日按约 6 小时专注开发、评审和测试计算；每周按 4 个有效人日，另留时间处理沟通、学习、平台手续和日常维护。

| 项目 | 估算 |
| --- | --- |
| V1 基础实施 | 60 人日 |
| 风险与返工缓冲 | 12 人日，约 20% |
| 总投入 | 72 人日，约 432 小时专注工作 |
| 每周 24 小时有效投入 | 约 18 周 |
| 每周 12 小时有效投入 | 约 36 周 |
| 外部等待 | 小程序审核、域名、供应商开通等另计，不伪装成人日 |

AI 用于生成初稿、辅助实现、补充测试用例和代码审查；估算不假设 AI 可以替代测试或将所有任务并行完成。实际人日偏差超过 20% 时重估剩余计划。

## 2. 推荐技术方案

### 2.1 技术栈基线

| 层次 | 建议选择 | 选择理由与约束 |
| --- | --- | --- |
| 小程序 | 原生微信小程序、TypeScript、WXML/WXSS | 只建设一个主客户端；减少跨端框架适配；基础库版本在 M0 真机验证后固定 |
| API | Python 3.12、FastAPI、Pydantic 2 | 以 Schema 校验和 OpenAPI 契约支撑 AI 输出与业务接口 |
| 数据访问 | SQLAlchemy 2、Alembic | 管理业务元数据、幂等记录、查询索引和迁移 |
| 数据库 | PostgreSQL 16 或以上受支持版本 | 事务、锁、任务队列和查询先集中在一个数据库 |
| 任务执行 | 独立 Python worker，PostgreSQL 持久队列 | 使用租约、心跳与 SKIP LOCKED 取任务，避免 V1 额外维护消息中间件 |
| Markdown | Markdown AST 解析器、ruamel.yaml 安全模式 | M0 验证往返保真、托管区更新和未知字段保留，再锁定库版本 |
| 中文检索 | PostgreSQL 参数化子串查询与 pg_trgm | 先验证实际中文样例；V1 不依赖英文分词默认配置 |
| 内容存储 | Linux 持久卷、原子替换、不可变版本对象 | 当前 Markdown 可直接阅读；文件哈希用于校验与恢复 |
| 模型与转写 | 供应商适配层、模拟实现 | 不把某个供应商写死到领域层；语音接入在 V3 |
| 部署 | Docker Compose、HTTPS 反向代理、独立备份位置 | 一台应用主机起步；上线前验证磁盘、备份与恢复能力 |
| 测试 | pytest、Vitest、微信开发者工具及真机 | 后端故障与隔离测试优先；小程序实际交互不用浏览器测试冒充 |
| 质量工具 | Ruff、ESLint、Prettier、TypeScript 类型检查 | 具体配置随首个代码提交进入仓库，依赖通过锁文件固定 |

本表是 D-15 的建议，不是已确认选型。M0 可根据开发者熟悉程度调整；调整后同步 Schema、工期和部署方案。

### 2.2 系统结构

~~~mermaid
flowchart LR
  C["微信小程序"] --> A["API：身份、校验、查询、提交任务"]
  A --> P["PostgreSQL：元数据、任务队列、索引"]
  A --> R["Capture 原文存储"]
  P --> W["Worker：分析、写入、导出、恢复"]
  W --> P
  W --> F["Markdown 当前文件与不可变版本"]
  W --> R
  W --> M["模型适配层"]
  P --> B["一致性备份"]
  F --> B
  R --> B
~~~

采用模块化单体：API 和 worker 是两个运行进程，共享领域服务与数据契约。外部 AI 调用不持有文件写锁；同 Workspace 的 Markdown 变更串行执行。

### 2.3 建议代码目录

下面是待创建的代码结构，与产品中的用户工作空间目录不同：

~~~text
GKD/
  AGENTS.md
  产品需求文档.md
  开发计划.md
  apps/
    miniprogram/
      src/
        pages/
        components/
        services/
        state/
        utils/
      tests/
      package.json
  services/
    api/
      app/
        api/
        domain/
        services/
        adapters/
        repositories/
        worker/
      tests/
        unit/
        integration/
        acceptance/
      migrations/
      pyproject.toml
  packages/
    contracts/
      openapi.json
      schemas/
  fixtures/
    captures/
    markdown/
    ai-evals/
  infra/
    compose.yaml
    env.example
  docs/
    architecture/
    runbooks/
    archive/
~~~

只有需要时才创建目录，不预建空的微服务或插件系统。实际结构、命令和测试说明以 `AGENTS.md`、README 与实现记录为准（`AGENTS.md` 已于 2026-09-10 重写）。

### 2.4 领域模块职责

| 模块 | 负责 | 不承担 |
| --- | --- | --- |
| identity / workspace | 登录、会话、租户归属、配置 | 业务负责人和团队权限推断 |
| capture | 原文持久化、来源、参照时间、提交防重 | 将原文直接改成 Task |
| markdown | 解析、校验、局部修改、渲染、文件 ID | 调用模型或决定用户意图 |
| changes | 差异、确认记录、版本校验、写入、撤销 | 自动接受模型建议 |
| tasks / folders | 业务规则、目录操作、GTD 视图 | 绕开 changes 直接写 Markdown |
| proposals / ai | 检索上下文、模型调用、输出校验、提案修订 | 直接执行任意文件路径或脚本 |
| search / index | 派生索引、查询、重建、损坏报告 | 成为 Task 内容的另一份权威数据 |
| jobs / operations | 队列、重试、导出、清理、备份、告警 | 在失败时静默丢弃业务记录 |

## 3. 数据与执行设计

### 3.1 存储布局与表

建议用户数据放在代码仓库之外，通过持久卷挂载：

~~~text
data/workspaces/<workspace_id>/
  files/
  captures/
  versions/objects/
  journals/
  trash/
  attachments/
~~~

files 保存当前可读 Markdown；captures 保存不可变原文 JSON；versions/objects 按内容哈希保存不可变版本；journals 保存跨文件写入恢复记录。attachments 在 V3 使用。版本对象的回收必须考虑当前文件、回收站、备份和未完成变更的引用。

数据库分两类：

| 类别 | 建议表 | 维护规则 |
| --- | --- | --- |
| 必须持久备份 | users、sessions、workspaces、folders、capture_records、proposals、proposal_revisions、change_sets、jobs、idempotency_records、audit_events、backup_epochs、deletion_tombstones | 不能通过重建 Markdown 索引替代备份 |
| 可重建投影 | group_index、task_index、tag_index、search_documents、relation_index | 业务写入后更新；重建只作用于这些投影 |

所有带用户数据的记录都包含 workspace_id。唯一约束至少包括：

- Capture：workspace_id + client_capture_id。
- 已确认提案：proposal_id + revision；同一修订只能对应一个应用批次。
- 幂等请求：user_id + route_scope + idempotency_key，同时校验请求体哈希。
- 业务对象：稳定 ID 唯一；同 Workspace 的规范化相对文件路径唯一。

身份、文件内容、操作记录和索引均有明确所有者，避免“重建索引时误清空用户或处理记录”。

### 3.2 Capture 保存

1. 校验登录、文本大小、client_capture_id 和请求体哈希。
2. 在数据库记录保存意图并预分配 Capture ID；相同 key 返回原意图或完成结果。
3. 将原文、来源、参照时间写入临时 JSON，flush/fsync 后原子替换到正式位置。
4. 提交元数据和保存状态，再返回 201；只有这时前端显示云端保存成功。
5. 崩溃后按意图记录与文件哈希恢复，清理无人引用的临时文件；不能按“数据库没有索引”就删除已确认的原文。
6. “立即整理”另建 Job；创建或执行 Job 失败不回滚已保存的 Capture。

本机待上传项持有原 client_capture_id。退出或切换账号时提示未上传数据，按账号隔离，并提供同步或明确清除操作。

### 3.3 Change Set 写入协议

这是 V1 的核心工程任务，必须有故障注入验证：

1. 从已确认的提案修订或明确的人工编辑请求构造 Change Set，冻结目标与动作。
2. 获取 Workspace 写锁；检查该空间不存在待恢复批次、资源仍可访问。
3. 再次比较每个目标的 revision、内容哈希以及提案保存时的规范化路径、目录版本和可写状态快照；过期则返回冲突，不自动重算并执行。
4. 生成全部 after 内容，校验 Schema、状态、容量和引用；持久保存 before/after 不可变版本。
5. 写入并持久化恢复日志，记录每个文件的旧路径、新路径、前后哈希以及该批次 ID；数据库记录 PREPARED。
6. 为该 Workspace 设置写入/恢复门禁；临时文件在目标同一文件系统内完成 fsync，再逐个原子替换。目录变更也记录恢复步骤。
7. 全部文件校验通过后标记 FILES_APPLIED，在数据库事务中更新投影、Capture/Proposal 状态、审计和 Change Set 结果。
8. 提交 COMMITTED，清理临时状态并解除门禁；前端查询 Job 得到成功与撤销入口。
9. 异常时，仍在锁和门禁保护下恢复 before 状态；需要人工处理时保持 RECOVERY_REQUIRED，不能继续接收同空间写入。
10. 服务启动时优先扫描 PREPARED、FILES_APPLIED 和 RECOVERY_REQUIRED；根据文件哈希完成提交或恢复，不凭状态名称猜测成功。

单文件原子替换不等于多文件事务。API 读取也必须遵守门禁：应用中或恢复中的 Workspace 返回短时可重试状态，避免一部分页面看到新文件、另一部分看到旧索引。

同一修订重复确认只查询原批次；租约过期重试必须先检查批次状态与磁盘内容。不要以“重新执行全部动作”替代恢复。

### 3.4 Job 与 AI 调度

Job 类型包括 analyze_capture、apply_changeset、export_workspace、reindex_workspace、purge_data；V3 增加 transcribe_audio。

- 使用 PostgreSQL 队列并设置租约、独立心跳、尝试次数与 next_run_at；建议心跳 15 秒、租约 90 秒，执行长任务时持续续租。
- 先领取并提交租约，再处理外部调用；不跨网络请求持有数据库事务。
- 同一 Capture 只允许一个有效分析任务；同一 Workspace 同时仅一个文件变更批次。
- V1 默认允许最多两个并行模型请求、一个文件写入执行器；队列压力经观测后再调节。
- 网络或供应商限流最多尝试三次，指数退避并带抖动；人工重试保留前次任务记录。
- 模型响应经 Schema、字段证据、资源归属和动作白名单校验后才转为 Proposal。
- 预算先预留、调用后按实际用量结算；异常释放预留，防止并发突破日预算。
- 账户删除、资源权限变化、提案过期在实际执行前重新检查。

### 3.5 索引、查询与重建

内容写入的主查询投影与 Change Set 提交一起更新，确保写后读取一致。V1 全文检索先使用参数化中文子串查询与 pg_trgm，短查询和无可用索引场景必须单独测量。

重建流程：枚举当前文件 → 读取并校验 Schema/ID → 写入隔离的临时投影 → 生成差异和损坏报告 → 获取短时写锁并切换。重建中继续保留旧投影，不能先清空线上索引。变更期间使用文件哈希或批次序号补齐增量后再切换。

不合法文件只隔离对应项；报告包含文件路径和修复建议，不自动“修复”用户正文。重建不触碰登录、Proposal、Job 等不可重建业务表。

### 3.6 导出、备份与恢复

导出和系统备份分开实现：导出是用户可携带的数据包；备份用于恢复整个服务。

- 导出固定一个已提交的工作空间版本，读取不可变内容对象，附带清单与校验和，避免打包过程中混入不同时间的文件。
- 备份建议每 30 分钟调度；最近一次有效备份年龄超过 1 小时立即告警，每日检查清单与存储可读性。
- 一致性备份采用共同 backup_epoch：短暂阻止新写入并等待在途提交完成，在同一边界取得数据库一致性快照与内容对象清单，再恢复写入。
- PostgreSQL 导出快照在备份事务存续期间交给 pg_dump 使用；引用的不可变内容对象在备份结束前固定保留，后台清理不得删除。
- 数据库备份与文件清单校验完整后才把 epoch 标记可恢复；备份写到独立故障域并加密，密钥不与备份明文放在一起。
- M0 验证此方案的小型原型，M5 实测写入暂停时间和备份时长；不能通过长时间只读来掩盖可用性不达标。
- 恢复时创建隔离环境 → 恢复同 epoch 的数据库和内容 → 校验哈希与恢复日志 → 重放删除清单 → 重建投影 → 跑核心验收 → 开放访问。
- 每次发布前演练一次恢复；运行期建议每月演练。只存在备份文件但没有恢复记录，不算达到 RPO/RTO。

## 4. API 与数据契约

### 4.1 通用约定

API 前缀为 /api/v1，字段使用 snake_case，时间规则遵循 PRD。认证主体从服务端会话取得；路径中的 workspace_id 必须再次授权。

列表采用 cursor + limit，默认 20、最大 100，返回 items、next_cursor。所有变更接口接受 Idempotency-Key；内容编辑附带 base_revision 和 base_hash。JSON Schema 与 OpenAPI 是客户端和后端共用的契约。

Capture 保存和提案编辑等轻量操作同步返回；Markdown/目录批次、分析、导出和重建返回 202 与 job_id，客户端轮询到成功后再显示完成。重复请求返回原资源或原任务状态。

### 4.2 V1 接口清单

| 方法与路径 | 用途 | 核心响应/要求 |
| --- | --- | --- |
| POST /auth/wechat | 一次性登录凭据换取本系统会话 | 校验真实微信响应；不把服务端密钥返回客户端 |
| POST /auth/logout | 退出当前会话 | 立即失效，处理本机账号数据 |
| GET /me | 当前用户与默认 Workspace | 仅返回必要的身份信息 |
| GET/PATCH /workspaces/{id}/settings | 时区、AI 开关、预算 | 修改供应商或处理范围时记录授权版本 |
| POST /workspaces/{id}/captures | 保存原文 | 201；client_capture_id 与原文哈希防重 |
| GET /workspaces/{id}/captures | Inbox 列表 | 状态、来源筛选和分页 |
| GET /captures/{id} | 原文与处理记录 | 校验所属 Workspace |
| POST /captures/{id}/analyze | 发起分析/重新分析 | 202；显式标记是否重新分析 |
| GET /jobs/{id} | 查询后台状态 | 状态、进度、可重试原因、结果资源 ID |
| POST /jobs/{id}/retry | 人工重试 | 仅允许可重试任务；保持业务幂等 |
| GET/PATCH /proposals/{id} | 查看/编辑提案 | 编辑需当前 proposal_revision，返回新修订和差异 |
| POST /proposals/{id}/confirm | 确认指定修订与动作 | 202；确认接口不接受额外任务字段 |
| POST /proposals/{id}/reject | 拒绝提案 | Capture 回到待处理，业务文件不变 |
| GET/POST /workspaces/{id}/groups | 列表/手动创建任务组 | 创建进入 Change Set |
| GET/PATCH /groups/{id} | 详情/修改属性与非托管正文 | 版本校验，禁止任意整文件覆盖 |
| POST /groups/{id}/tasks | 手动新增 Task | 目标组版本校验 |
| GET /workspaces/{id}/tasks | 筛选、GTD 视图 | 与 PRD 第 5.4 节一致 |
| GET/PATCH /tasks/{id} | 详情/编辑、完成、延期 | 通过所在 Group 的版本约束写入 |
| GET/POST /workspaces/{id}/folders | 浏览/创建目录 | 父目录校验，创建返回 Job |
| PATCH /folders/{id} | 改名、移动目录 | 拒绝循环、越权、冲突路径 |
| POST /groups/{id}/move | 移动文件 | 目标目录与内容版本校验 |
| GET /workspaces/{id}/tags | 读取已使用标签 | V1 标签分配通过 Group/Task 编辑 |
| GET /workspaces/{id}/search | 中文全文检索 | q、类型与结构化过滤；隔离和分页 |
| GET /groups/{id}/versions | 文件历史 | 时间、来源、差异与恢复信息 |
| POST /change-sets/{id}/undo | 撤销变更批次 | 当前哈希匹配时执行，否则返回冲突 |
| POST /groups/{id}/restore-preview | 预览旧版本恢复 | 返回恢复提案；仍通过 confirm 执行 |
| POST /workspaces/{id}/archive | 归档/恢复指定对象 | body 指定对象 ID 与方向；目标仍须授权 |
| POST /workspaces/{id}/trash | 移入回收站 | 展示并校验子对象数量 |
| GET /workspaces/{id}/trash | 查看回收站 | 类型、到期时间、恢复信息 |
| POST /trash/{id}/restore | 恢复对象 | 名称冲突时返回可处理错误 |
| DELETE /trash/{id} | 永久删除 | 再确认凭据与影响范围校验 |
| POST /workspaces/{id}/exports | 发起导出 | 202；确认包含版本或附件的选项 |
| GET /exports/{id}/download | 下载已完成导出 | 所有权校验和短时凭据，过期后不可访问 |
| POST /workspaces/{id}/reindex | 启动检查/重建 | 202；不允许影响不可重建业务表 |
| DELETE /workspaces/{id} | 删除整个工作空间 | 重新认证、确认范围、异步清理并立即阻止访问 |
| DELETE /me | 删除账号和所属数据 | 与 Workspace 删除流程复用并撤销全部会话 |

这些路径为实施契约建议，M0 生成 OpenAPI 时冻结。健康检查和运行指标使用独立受控入口，不混入普通用户数据 API。

### 4.3 错误与重试语义

统一错误响应：

~~~json
{
  "error": {
    "code": "VERSION_CONFLICT",
    "message": "文件已被修改，请重新预览后确认。",
    "request_id": "req-a93dca3e-83fd-4f3b-b208-7533b419eea5",
    "retryable": false,
    "details": {
      "current_revision": 4,
      "submitted_revision": 3
    }
  }
}
~~~

| HTTP 状态 | 错误类型 | 客户端处理 |
| --- | --- | --- |
| 400 / 422 | INPUT_INVALID / SCHEMA_INVALID | 标出字段；不自动重试 |
| 401 | SESSION_EXPIRED | 重新登录，保留当前账号未提交草稿 |
| 403 / 404 | FORBIDDEN / NOT_FOUND | 不暴露其他 Workspace 的资源是否存在 |
| 409 | VERSION_CONFLICT / IDEMPOTENCY_MISMATCH / PROPOSAL_STALE | 重新查询或预览；不能自动覆盖 |
| 413 | INPUT_TOO_LARGE | 提示拆分，保留完整原文 |
| 429 | RATE_LIMITED / AI_BUDGET_EXCEEDED | 显示重试时间或用量；保存 Capture 不受模型预算阻断 |
| 503 | AI_UNAVAILABLE / WORKSPACE_RECOVERING / STORAGE_UNAVAILABLE | 展示原因；仅在 retryable=true 时使用同一幂等键重试 |

已入队任务的运行失败通过 Job 响应报告，不伪装成请求提交失败。

## 5. V1 里程碑与依赖

相邻阶段可以在同一自然周衔接，表中的周次不是多人并行安排。每个阶段以验收门槛完成为准，不能仅因日历到期就视为交付。

| 里程碑 | 参考周次 | 基础人日 | 交付物 | 进入下一阶段的门槛 |
| --- | --- | --- | --- | --- |
| M0 需求与契约 | W1—W2 | 5 | 决策记录、页面流程、Schema、30 条样例、格式与备份小型验证 | 关键决策已记录，Task 格式往返验证，核心流程无未解决矛盾 |
| M1 工程底座与收集 | W2—W4 | 9 | 开发环境、CI、登录隔离、原文保存、本机待上传、后台队列 | 原文保存与重启恢复通过，跨用户读取被拒绝 |
| M2 文件与变更引擎 | W4—W7 | 13 | Markdown 服务、手工编辑、版本、撤销、幂等、恢复 | AT-04/05/07/08 核心故障测试通过，手工闭环可演示 |
| M3 AI 整理闭环 | W7—W10 | 12 | 模型适配、检索、提案、确认页、用量和评估报告 | 提案只经确认写入；冻结集达标或明确记录真实供应商尚未验收项 |
| M4 功能完整与体验 | W10—W13 | 12 | GTD、目录、标签、中文检索、导出、回收站、隐私设置 | V1 功能清单逐项通过，两种系统真机核心路径可用 |
| M5 质量与发布 | W13—W15 | 9 | 回归、性能、安全、部署、恢复演练、内测反馈 | PRD 第 14 节门槛全通过，包括真实 AI 评估和至少 7 天内测 |
| 缓冲与发布窗口 | W16—W18 | 12 | 修复、回归、内测观察与发布支持 | 无阻断问题；外部平台条件满足 |

M3 中如果缺少外部 AI 授权或可用服务，可以完成模拟服务下的工程验证，但不能把真实效果验收标成通过。M4 候选版本具备必要安全与恢复能力后可开始内测，让观察周期与 M5 修复衔接。

关键依赖：

~~~mermaid
flowchart LR
  M0["M0 格式与契约"] --> M1["M1 原文与隔离"]
  M1 --> M2["M2 可靠写入与撤销"]
  M2 --> M3["M3 AI 提案与确认"]
  M3 --> M4["M4 功能完整"]
  M4 --> M5["M5 验证与发布"]
  E["AppID、域名、AI 授权、部署条件"] --> M5
~~~

## 6. 工作分解与完成标准

全部任务由同一开发者负责执行和审查，AI 作为辅助工具。依赖表示可以开始的条件，实际排期仍按个人容量安排。人日已含该任务的基本测试与文档，不在末期才补齐正确性。

### 6.1 M0：5 人日

| 工作项 | 人日 | 依赖 | 交付与完成标准 |
| --- | --- | --- | --- |
| DEV-001 需求冻结与发布条件核查 | 1 | 无 | 冻结 D-01/02/03/06/15；记录 D-12/18 的办理条件、预算与风险；形成 V1 验收清单 |
| DEV-002 Schema 与存储技术验证 | 2 | DEV-001 | 定义 Task/Group/TimeSpec/Proposal/Change Set；验证 YAML 往返、内容版本及共同备份快照；生成初版 OpenAPI；先以最小独立验证程序完成，不依赖 DEV-004 的完整工程 |
| DEV-003 页面流程与样例 | 2 | DEV-001 | 画出收集、确认、完成、撤销和错误状态；整理 30 条脱敏样例与预期结果；可模拟完整流程 |

### 6.2 M1：9 人日

| 工作项 | 人日 | 依赖 | 交付与完成标准 |
| --- | --- | --- | --- |
| DEV-004 工程、环境与 CI | 2 | DEV-002 | 建立实际目录、依赖锁、测试环境、迁移与质量检查；新环境按文档可启动 |
| DEV-005 登录与 Workspace 隔离 | 2 | DEV-004 | 登录、会话、默认空间、退出；所有资源查询以 Workspace 授权；越权用例通过 |
| DEV-006 Capture 保存与 Inbox | 3 | DEV-003、DEV-005 | 原文保存协议、本机待上传、文字粘贴、列表和详情；崩溃、断网、重试后原文一致且不重复 |
| DEV-007 Job 与运行状态底座 | 2 | DEV-004、DEV-005 | 持久队列、租约、心跳、重试、状态查询和基础监控；进程退出后任务可恢复 |

### 6.3 M2：13 人日

| 工作项 | 人日 | 依赖 | 交付与完成标准 |
| --- | --- | --- | --- |
| DEV-008 Markdown 解析与局部更新 | 3 | DEV-002、DEV-004 | ID、Front Matter、托管任务区、未知字段、Unicode 路径和格式校验；往返样例通过 |
| DEV-009 Change Set 与恢复协议 | 4 | DEV-007、DEV-008 | 锁、幂等、版本检查、不可变快照、恢复日志、读写门禁；多文件失败恢复有自动验证 |
| DEV-010 手工任务接口与最小页面 | 3 | DEV-005、DEV-009 | 新建、追加、修改、完成、延期和撤销；通过页面可完成最小手工闭环，审计可追溯 |
| DEV-011 故障测试与索引重建 | 3 | DEV-009、DEV-010 | 逐阶段故障注入、并发冲突、恢复与增量重建；不修改权威文件，不清空不可重建表 |

### 6.4 M3：12 人日

| 工作项 | 人日 | 依赖 | 交付与完成标准 |
| --- | --- | --- | --- |
| DEV-012 模型适配与上下文检索 | 3 | DEV-006、DEV-007、DEV-010 | 模拟/真实适配器、供应商配置、最小上下文、缓存隔离；关闭模型仍可手工操作 |
| DEV-013 分类与结构化提案 | 3 | DEV-012 | 分类、字段、时间、置信度、证据、Schema 校验、澄清；无效输出不能进入写入层 |
| DEV-014 提案编辑、预览与确认 | 4 | DEV-003、DEV-009、DEV-013 | 确认页、字段编辑、归属选择、修订绑定、选中动作、取消与执行状态；重复确认和旧版本测试通过 |
| DEV-015 AI 评估、预算与降级 | 2 | DEV-013、DEV-014 | 80 条分层样例及留出评估；用量预留/结算、超时和重试；结果达标或留下明确未验收记录 |

### 6.5 M4：12 人日

| 工作项 | 人日 | 依赖 | 交付与完成标准 |
| --- | --- | --- | --- |
| DEV-016 Task、目录与 GTD 页面 | 4 | DEV-010、DEV-014 | 完整任务详情、今日视图、文件夹浏览/移动/归档；时区、状态、目录引用行为与 PRD 一致 |
| DEV-017 中文搜索、筛选与标签 | 2 | DEV-011、DEV-016 | 标题/正文/Capture 检索、过滤、分页和单对象标签；中文短词、空结果、越权测试通过 |
| DEV-018 导出、删除与隐私设置 | 3 | DEV-009、DEV-016 | 一致导出、回收站、账户删除、清理任务、AI 授权和用量设置；校验和及删除用例通过 |
| DEV-019 真机体验与异常状态 | 3 | DEV-016、DEV-017、DEV-018 | iOS/Android 真机记录；离线、长文、键盘遮挡、回到前台和账户切换可恢复；形成内测候选版 |

### 6.6 M5：9 人日

| 工作项 | 人日 | 依赖 | 交付与完成标准 |
| --- | --- | --- | --- |
| DEV-020 集成回归与性能安全验证 | 3 | DEV-011、DEV-015、DEV-019 | AT-01—AT-12、目标数据量压测、权限与路径攻击验证；阻断/高严重度问题清零 |
| DEV-021 部署、备份与恢复演练 | 3 | DEV-018、DEV-020 | 测试/生产配置、HTTPS、告警、有效备份、空环境恢复和版本回退演练；满足 RPO/RTO |
| DEV-022 内测修复与发布资料 | 3 | DEV-015、DEV-019、DEV-020、DEV-021 | 至少 7 天内测观察、缺陷处理、使用说明、隐私说明和平台提交材料；逐项签核发布门槛 |

DEV-022 的 3 人日是处理反馈和准备资料的工作量，7 天是观察跨度，二者不能互相替代。平台审核等待也不能算作任务已通过。

基础工作量校验：5 + 9 + 13 + 12 + 12 + 9 = 60 人日；缓冲独立列为 12 人日，不分摊为“已完成”。

## 7. 需求覆盖与交付映射

| 需求范围 | 首次完整验收阶段 | 主要工作项 |
| --- | --- | --- |
| FR-001、FR-003、FR-004、FR-006 | M1 | DEV-006；后续界面完善在 DEV-019 |
| FR-024、FR-037 | M1/M4 | DEV-005、DEV-006、DEV-019 |
| FR-011、FR-012、FR-013、FR-014、FR-028、FR-036 | M2 | DEV-008—DEV-011；AI 触发路径在 DEV-014 |
| FR-007、FR-008、FR-009、FR-027、FR-032 | M3 | DEV-007、DEV-012—DEV-015 |
| FR-015、FR-016、FR-017、FR-019、FR-020、FR-025 | M4 | DEV-010、DEV-016 |
| FR-021、FR-022、FR-026、FR-033 | M4 | DEV-011、DEV-017 |
| FR-029、FR-030、FR-031、FR-038 | M4/M5 | DEV-015、DEV-018、DEV-019、DEV-021 |
| FR-005、FR-010、FR-018、FR-034、FR-035，以及 FR-013/026 的增强部分 | V2 | 见第 12 节 |
| FR-002 | V3 | 见第 12 节 |
| FR-040 | V4 | 见第 12 节 |
| FR-023、FR-039 | V5 | 见第 12 节 |
| NFR-01—NFR-11、AT-01—AT-12 | M5 总体验收 | 各工作项自测，DEV-020—DEV-022 汇总 |

表中“首次完整验收”不表示测试推迟到该阶段；例如 Workspace 隔离和 Capture 幂等从 M1 起即加入持续回归。

## 8. 测试与质量策略

### 8.1 分层测试

| 层次 | 工具与对象 | 必须覆盖的风险 |
| --- | --- | --- |
| 单元 | pytest；解析、时间、状态、动作校验 | 非法 YAML、未知字段、重复 ID、托管区冲突、午夜/时区边界、依赖循环 |
| 客户端逻辑 | Vitest；请求状态、本机待上传、过滤与时间展示 | 超时后重复提交、账户切换、过期提案、前后台切换 |
| 契约 | JSON Schema / OpenAPI 校验 | 客户端与 API 字段漂移，缺省值、枚举、不允许的动作和版本 |
| 集成 | pytest + 真实测试 PostgreSQL + 临时目录 | 事务、唯一约束、文件锁、目录移动、索引与文件一致性、清理任务 |
| 故障与并发 | 可控故障注入 | 每个持久化边界中断，两个编辑竞争，租约到期，重复确认，数据库或磁盘失败 |
| AI 评估 | 固定样例和输出报告 | 无依据填值、上下文误匹配、指令注入、隐私范围、真实供应商效果 |
| 小程序交互 | 开发者工具自动化能力 + 人工真机 | 微信登录、键盘、授权、网络切换、长列表、回到前台与错误恢复 |
| 运维验收 | 隔离环境演练与压测 | 一致备份、恢复、删除清单、回滚、告警和目标数据量 |

V1 只在相关功能支持时测试依赖循环等 V2 行为；预留字段至少拒绝不支持的操作，不能通过接受字段暗示功能已经实现。

### 8.2 关键故障矩阵

至少在以下位置强制中断并重启：Capture 文件落盘前后、Change Set PREPARED 后、第一和最后一个文件替换后、索引事务提交前后、成功响应返回前。

每个用例验证：

1. 已确认原文与内容哈希一致。
2. 同一幂等键没有新增重复对象。
3. 读写门禁在恢复结束前有效。
4. 结果为全部提交或全部恢复，任务和文件可互相追溯。
5. 用户继续编辑与撤销后仍可正常完成操作。

另外覆盖账户删除发生在 AI 排队、模型返回、文件写入之前的情况；未经授权的后台任务不能继续执行。

### 8.3 持续集成门槛

每次变更运行格式、静态检查、类型检查、单元、契约和受影响集成测试。涉及 changes、身份或持久化时必须运行完整故障与越权回归。

建议后端领域层行覆盖率至少 85%；覆盖率仅用于发现遗漏，不以堆积断言代替行为验收。AT 用例与安全/恢复失败不能用整体覆盖率掩盖。

模拟 AI 用于日常确定性测试；真实 AI 冻结集在模型、提示词、Schema 或检索策略变化时运行。真实服务耗时和费用单独记录，不让普通单元测试依赖外部账号。

测试数据库与存储目录必须与开发/生产隔离；测试启动时校验目标配置，测试凭据不得拥有生产权限。小程序发布前保留真机验证记录，不把 TypeScript 编译成功当作界面验收。

## 9. 开发环境与命令约定

以下命令需要 DEV-004 建立对应文件和脚本后才能执行；当前仓库没有可运行的构建或测试命令。

| 工作目录 | 计划命令 | 用途 |
| --- | --- | --- |
| 仓库根目录 | `docker compose -f infra/compose.yaml up -d db` | 启动开发数据库 |
| services/api | `uv sync --dev` | 按锁文件安装后端及测试依赖 |
| services/api | `uv run alembic upgrade head` | 应用开发数据库迁移 |
| services/api | `uv run uvicorn app.main:app --reload` | 启动本地 API |
| services/api | `uv run python -m app.worker` | 启动后台任务；模块应提供明确入口 |
| services/api | `uv run ruff check .` | 后端静态检查 |
| services/api | `uv run ruff format --check .` | 检查格式 |
| services/api | `uv run pytest tests/unit -q` | 单元测试 |
| services/api | `uv run pytest tests/integration -q` | 使用隔离数据库的集成测试 |
| services/api | `uv run pytest tests/acceptance -q` | AT 行为与故障回归 |
| apps/miniprogram | `npm ci` | 按锁文件安装前端依赖 |
| apps/miniprogram | `npm run typecheck` | TypeScript 类型检查 |
| apps/miniprogram | `npm run lint` | 前端静态检查 |
| apps/miniprogram | `npm test` | Vitest 非交互测试 |
| apps/miniprogram | `npm run build` | 输出供微信开发者工具加载的构建文件 |

首次建立工程时选择并固定包管理器版本、创建锁文件和上述脚本；不要提前把未创建的命令标为已通过。

开发者工具负责预览与真机调试，API 测试端点和小程序合法域名按平台实际要求配置。本地开发便利选项不得直接带入生产。

env.example 只记录名称和非敏感示例，至少涵盖 DATABASE_URL、TEST_DATABASE_URL、DATA_ROOT、WECHAT_APP_ID、WECHAT_APP_SECRET、SESSION_SECRET、AI_PROVIDER、AI_API_KEY、AI_DAILY_BUDGET、BACKUP_TARGET 和 BACKUP_KEY 的用途；密钥实际值不提交仓库。

## 10. 环境、发布与运行维护

### 10.1 环境隔离

| 环境 | 数据与外部服务 | 用途 |
| --- | --- | --- |
| 本地开发 | 脱敏样例、独立数据库、默认模拟 AI | 实现与快速调试 |
| 自动化测试 | 每次测试隔离数据库/临时目录，无生产权限 | 契约、并发、故障和回归 |
| 预发布 | 与生产同结构、测试账号、受预算限制的真实 AI | 真机、性能、恢复和发布演练 |
| 生产 | 独立凭据、持久卷、真实私有数据、有效备份 | 内测与正式使用 |

配置与密钥通过环境或密钥管理方式注入。开发环境不能复用生产 DATA_ROOT；生产不能开启调试、宽松认证或公开数据库端口。

### 10.2 发布流程

1. 选定具体版本，生成变更说明、迁移影响和验收记录。
2. 在预发布部署相同构建，运行核心验收、真实 AI 评估与必要真机检查。
3. 验证最新一致性备份可用，记录当前应用版本、Schema 和数据库迁移版本。
4. 后端先发布兼容接口；数据库优先采用新增字段/表等可兼容迁移，再发布小程序。
5. 用测试账号验证登录、收集、确认、读取、完成和撤销；观察错误率与后台积压。
6. 先向小范围内测用户开放，满足 PRD 发布准入后提交相应平台版本。
7. 发布后检查首批真实请求、备份时效和清理任务，记录需要跟进的问题。

小程序客户端可能滞留旧版本，后端需保留已发布 API 的兼容行为；字段删除和状态语义调整必须经过单独迁移，不和同日客户端更新绑定。

### 10.3 回滚与事故处理

应用回滚优先退回上一兼容镜像，不随意回退或删除业务数据。旧程序不能读取新 Schema 时保持只读或维护状态，执行已验证的修复/恢复方案。

出现数据丢失、越权或不可恢复的半批次状态时：停止相关写入 → 保留日志和故障现场 → 定位受影响 Workspace 与批次 → 恢复并校验 → 回归同一故障用例 → 再开放。不能通过删除错误文件让测试“重新通过”。

监控至少覆盖 API 错误率、P95、Job 最长等待时间、恢复中工作空间数、AI 失败与额度、磁盘剩余量、最近有效备份时间和删除任务逾期。告警必须对应可执行的处理说明。

### 10.4 成本模型

供应商、区域和预算未确定，当前不提供未经验证的价格。正式接入前记录以下实时报价和限额：

| 项目 | 估算方式 | 控制方式 |
| --- | --- | --- |
| 计算 | 应用/数据库主机月费 + 流量 | 首次压测以 PRD 基准配置为参照，降配后重新验证 |
| 存储 | 当前内容 + 版本 + 回收站 + 导出临时包 | 按保留规则清理可回收对象，监控总占用 |
| 备份 | 去重后的内容量 + 数据库备份 + 跨区域流量 | 30 天留存、完整性检查和独立密钥 |
| AI | 输入 token × 输入单价 + 输出 token × 输出单价 | 每日预算、单请求上限、缓存和显式重试 |
| 转写 | 音频分钟数 × 单价 | V3 按用户/日期限额，失败与重复请求去重 |
| 平台与域名 | 所选主体、域名和平台实际费用 | M0 核查，未开通前不承诺发布时间 |

内测记录“每 100 条 Capture 的模型费用、平均输入长度、任务成功率与存储增量”，用实测值决定下一阶段预算。关闭 AI 不应阻止手工使用。

## 11. 风险登记与应对

| 风险 | 影响与发现时点 | 应对与调整触发 |
| --- | --- | --- |
| Markdown 往返丢内容 | M0/M2；破坏事实源 | 使用固定样例验证未知字段、正文与托管区；不达标先修数据层，推迟后续功能 |
| 多文件与数据库状态不一致 | M2；数据损坏 | 恢复日志、门禁、幂等、故障注入；未通过不得接入自动生成的批量动作 |
| 备份无法同时恢复文件和元数据 | M0 原型/M5 演练 | 固定共同 epoch 与不可变对象；真实恢复达不到 RPO/RTO 时调整方案，不能只检查备份任务返回码 |
| AI 准确率不足或成本过高 | M3；提案不可用 | 测冻结集、缩小上下文、增加澄清、调整模型；不降低确认与隔离标准 |
| 小程序登录、网络或平台能力受限 | M0/真机验证；发布阻塞 | 尽早核查 AppID、域名、主体与实际接口；平台特有增强延期，不影响手工文字闭环开发 |
| 中文查询性能不达标 | M2/M4；搜索体验差 | 短词、长文本和目标规模实测；需要分词或独立索引时作为显式工作项重估 |
| 目录移动与引用更新范围过大 | M2/M4；长时间占用工作空间 | 预估影响数量、后台进度、批次恢复；先完成 V1 常见目录规模，压测异常规模 |
| 原始聊天或密钥进入日志 | 全阶段；隐私泄露 | 默认脱敏、日志字段白名单、测试环境隔离；相关问题按高严重度阻断发布 |
| 单人维护压力与范围膨胀 | 每周；工期失控 | 只完成当前里程碑；新增 P0 同时增加预算或替换范围；偏差超过 20% 重新排期 |
| 没有足够内测反馈 | M4；效果无法验证 | 提前准备目标用户与场景；无法获得真实观察时如实标为未完成，不用模拟反馈替代 |
| 数据盘耗尽或后台任务失控 | M1/M2/M5；服务不可写 | 空间预检、配额、租约与重试上限、积压告警；不自动删除原始输入 |
| 全文源码编辑扩大冲突范围 | V2；覆盖用户内容 | 导入先试运行、Schema 和任务视图冲突检查；不能沿用 V1 结构化编辑的简单假设 |

风险缓冲优先处理数据、安全与运行问题。若出现新的客户端、团队权限或双向同步需求，应重新立项和估算，不直接塞入 12 人日缓冲。

## 12. V2—V5 后续开发路线

后续工作量是范围级估算，需在相应版本开始前根据 V1 结果重新拆分。下列参考周数已按每周 4 个有效人日和约 20% 缓冲计算，平台等待另计。

| 版本 | 基础人日 | 参考周期 | 核心工作包 | 准入与验收 |
| --- | --- | --- | --- | --- |
| V2 关系与结构增强 | 20—28 | 6—9 周 | 多 Capture 提案、Task 拆解、父子/依赖图、反向链接、标签批量操作、Markdown 导入/源码编辑 | V1 稳定；导入试运行与冲突恢复通过；关系无循环、无越权、移动不丢 ID |
| V3 录音与转写 | 10—16 | 3—5 周 | 麦克风权限、录音生命周期、可恢复上传、音频私有存储、转写适配、原音频追溯 | 供应商与真机能力验证；权限拒绝、后台中断、转写失败不丢音频 |
| V4 微信工作流扩展 | 12—20 | 4—6 周 | 平台能力验证、可用的分享入口、企业微信接入、来源授权、消息去重、解绑 | 只实现验证可用的接口；接入范围与留存方式明确；重复回调不重复建任务 |
| V5 回顾与语义能力 | 16—24 | 5—8 周 | 回顾规则、长期未处理分析、重复任务建议、语义索引、自然语言检索与效果评估 | 有足够真实使用样例；建议可追溯，修改仍确认，语义结果不越权 |

V1 到 V5 全路线粗略为 36—46 周，前提是人员投入不变、后续范围不扩大；这不是全部版本的固定交付承诺。语音如果被证明是首要入口，可以在 V1 稳定后先做 V3，但应明确交换 V2 的交付次序。

### 12.1 V2 的实现顺序

先建立稳定 ID 关系和循环检测，再做父子任务与反向链接；之后接入批量 Capture 和 AI 拆解，最后开放导入和源码编辑。全局标签改名/合并通过可恢复批次执行，不直接批量替换文件字符串。

导入需生成清单、试运行差异、重复 ID 处理建议和任务视图冲突报告。用户选择规则后才应用，导入失败可整批恢复。

### 12.2 V3 的实现顺序

先用真实手机验证录音格式、时长、权限和前后台限制，再确定产品上传上限；不直接把草稿里的暂停、继续录音当作所有设备都已支持。

音频上传与转写分离；上传完成后才创建转写任务；转写版本和原音频保持关联。中断上传、重复回调、余额不足与供应商失败各有恢复路径。

### 12.3 暂无排期的能力

多人共享权限、实时协作、Git/Obsidian 双向同步、完整日历同步、支付订阅、知识图谱可视化和图片/PDF 深度理解不包含在以上人日内。它们需要独立的用户需求和架构评估。

## 13. 首个两周的执行清单

两周按 8 个有效人日安排，产物用于尽早验证投入方向：

| 顺序 | 工作 | 有效人日 | 可检查产物 |
| --- | --- | --- | --- |
| 1 | 执行 DEV-001 | 1 | 关键决策记录、平台条件清单、V1 验收范围 |
| 2 | 执行 DEV-002 | 2 | Schema、格式往返与快照验证结果、初版 OpenAPI |
| 3 | 执行 DEV-003 | 2 | 页面流程、30 条带预期结果的样例、模拟操作 |
| 4 | 执行 DEV-004 | 2 | 可启动工程、锁文件、测试入口、CI 与开发说明 |
| 5 | 开始 DEV-005 | 1 | 身份模型、Workspace 归属校验和会话契约；余下真实登录/隔离验收在后续完成 |

两周评审应能回答：文字输入能否顺畅收集，数据格式是否保真，确认与恢复是否能实现，真实 AI/平台接入还有哪些外部条件。若这些基础问题尚未解决，先调整方案，避免继续堆叠页面。

## 14. 工作项管理与完成定义

### 14.1 开始条件

每项开发工作先明确关联 FR/AT、输入输出、异常行为、前置依赖和测试数据。涉及持久化格式或接口的变化先更新契约；涉及外部服务先明确测试账号、费用边界和数据处理范围。

### 14.2 完成条件

一个工作项只有同时满足以下条件才记为完成：

- 对应功能可运行，关键异常可复现且有处理结果。
- 相关自动化测试、静态检查和必要的真机检查通过。
- 接口、迁移、配置与使用说明同步更新。
- 没有未说明的数据兼容性或恢复风险。
- 留下可复查的验收记录；已知问题标明严重度、影响和后续工作项。

### 14.3 每周跟踪

每周记录本周完成的 DEV 编号、验收证据、实际人日、阻塞条件、剩余估算和下周目标。任务状态使用未开始、进行中、待验收、已完成、受阻；“代码已生成”不等于“已完成”。

当前 DEV-001—DEV-022 均为待实施计划。下一阶段从 DEV-001 的基线与外部条件核查、DEV-002 的格式和存储验证开始，具体开工日期按实际投入确定。
